{
  "name": "tk-suite",
  "private": true,
  "version": "0.0.0",
  "description": "TK 养号 + 发布自动化 — pnpm monorepo (autotk / management-center / license / signing-station / telemetry)",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck"
  }
}
