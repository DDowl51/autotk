# 真机与上线收尾清单

二期功能的**代码 + 单测/集成测**已完成（见 `management-center/docs/dev-phase23.md`、`autotk/src/*/README.md`）。
本清单只列**还需要真机 / Mac 构建 / 实机联调 / 上线部署**才能验收的事项，拿到手机后逐项过。

更新日期：2026-06-28。

---

## 0. 前置：WDA 在 iPhone 8 / iOS 16 上跑起来
> 没有这一步，下面所有 autotk 真机验收都做不了。详见 `autotk/WDA_STANDALONE.md`。

- [ ] 云编译出 `WebDriverAgent.ipa`（GitHub Actions，Xcode 14.3.1）
- [ ] 装到手机：免费账号走 Mac 上 Sideloadly（7 天）／付费账号走 Linux zsign（1 年）
- [ ] `go-ios image auto` 挂镜像（iOS16 不要 tunnel）+ `runwda` 起到 `:8100`
- [ ] **重新 `calibrate`**：iPhone 8 是 **375×667**，与原机 390×844 不同，坐标必须重标

---

## 1. autotk（手机端，RN/Expo）—— 需 Mac 构建
> autotk 的纯 TS 逻辑都过了 node:test；但所有 **RN 接线 + 原生模块**只能在 Mac 上 `expo` 构建后真机验。

- [ ] `expo start` / dev build 跑通：配置页（关键词/推荐/搜索/个人主页/时间/日志）、启动/停止
- [ ] **后台保活**（keepalive：始终定位）真机验——退后台 30s 不被挂起
- [ ] **#2 固定回复**：UI 列表 + 占位符 `{emoji}/{user}/{a|b|c}/{kw}` 真机展开
- [ ] **#4 弹窗脱困**：REPL `alert` 命令验 WDA 14.x 的 `/alert` 接口好不好使；**不行则退回 OCR**
- [ ] **应用内浮层检测/脱困**（`popupDetect.ts`+`popupDismiss.ts`+`detectCardClose`）：已按真机截图加了三个实测浮层签名——**接收好友新作品通知 / 虚拟头像 / 虚拟物品和奖励政策**（后者点「知道了」安全关，前两者点卡片右上 ✕）；真机验：这三个能自动关掉、`popup_detected`/`popup_escaped` 埋点成功率高。**待调**：`detectCardClose`（卡片 ✕ 视觉定位）的灰度/区域阈值是起步值，真机核实点得准不准；再遇到新浮层按同法加签名。onDeviceUI 与 calibratedUI（REPL）两处都接了脱困
- [ ] **直播卡跳过（本轮新增）**：推荐/关注流遇到**直播卡不互动、直接下滑**（`isLivePage` 认「直播中/点击进入直播间」，只看下半屏避开顶部 LIVE tab）。真机验：刷到直播不点赞/不进评论/不误入直播间；`isLivePage` 的标记词/区域阈值真机核（尤其英文界面的直播标记）
- [ ] **#3 评论 OCR 回复**：真机调坐标/阈值——评论区域、行间距、爱心↔评论对齐、Reply 入口坐标；看「评论解析:N 条」日志调
- [ ] **#5 激活码门禁**：RN 接线（Keychain 存 token、激活页、心跳）真机验；上线前到 license 后台建 `autotk` 产品，把 key/secret 填进 `autotk` config 或 `.env`
- [ ] **Hub 接入**（状态/日志/批量配置/发布接收）：HubClient / useEngine / configInbox / publish 接收的 RN 接线真机验；`EXPO_PUBLIC_HUB_URL` 指向 Hub
- [ ] **关注监控打粉（本轮新增 A4）**：真机标定顶部「关注」Tab 坐标（现为 `anchors.ts` 的 `followTab` 比例默认 `x≈0.38 / y≈0.065`，可在 devices.json 的 `anchors` 逐点覆盖）；验证关注流里评论 OCR 与命中回复；REPL 跑通、看 `run` 时「关注监控」与养号交替
- [ ] **多页面锚点标定（本轮新增，1+3+4）**：`calibrate` 现只标右栏 like/comment/save/share；**其余页面锚点**（`followTab / searchIcon / searchSubmit / searchFirstResult / replyEntry / commentInput / profileTab / gridFirstCell / backArrow / homeTab / publish*`）走 `src/engine/anchors.ts` 的**比例默认**，真机需逐族核实并在 `devices.json` 的 `anchors` 覆盖偏的点（尤其非 390×844 比例家族：375×667 / 393×852 / 430×932 / iPad）
  - [ ] **标定正确性校验**：`validateRail`（靠右缘/同列/y 递增/间距均匀）已接进 `calibrate`——真机验：故意停在非视频页 calibrate 应被拒、不写盘
  - [ ] **首跑自动标定**：无精确档时 `realUI` 自动检测+校验+存 AsyncStorage（`deviceProfileStore`）；否则用「最接近档」兜底（`nearestProfileKey`，不再盲取第一个）。真机验：换个未标定分辨率的机器，停在干净视频页启动 → 自动标定成功并记住
  - [ ] **发布上传流程标定（#4）**：`publishVideo` 已实现骨架但 `publish*` 锚点为占位，**必须真机 calibrate/手改覆盖后才可用**——否则会点偏甚至误发。真机逐步核 `+→上传→选片→下一步→文案→发布` 每个锚点
- [ ] **右栏运行时重定位（治 A，本轮新增）**：赞/藏/评/享不再读死坐标——每条视频进来按当前白带位置整体吸附（`railBandCenters`+`railOffsetY`，纯逻辑已测）。真机验：**长文案/短文案视频交替**时四个动作都点得准（不再因逐视频 ±25px 浮动点空）；`railOffsetY` 的 `maxShift`（默认 60）等阈值真机调；`onDeviceUI` 与 `tools/calibratedUI` 两处行为一致
- [ ] **白带检测抗白背景（治 B，本轮新增）**：`looksLikeIconRow` 拒绝「整行边到边发白」的亮场背景（白墙/雪景）误检（纯逻辑已测）。真机验：在**白/亮背景视频**上 `calibrate`/`detect` 不再误检出假图标带；`edge` 阈值（默认 4）真机调
- [ ] **OCR 锚点定位（本轮新增，已接线待真机）**：`calibrate` 现会用电脑 **tesseract**（`tools/ocr.ts` 的 `recognizeBoxes` 词级框 + `anchorLocate.findAnchorByText`）按文字定位顶部「关注/Following」Tab，命中就写进 `devices.json` 的 `anchors.followTab`（比比例猜稳、跨机型跨版本；只看顶部带避开评论同字）。真机验：`calibrate` 日志出现「OCR 定位「关注」Tab → (x,y)」且点得准；首次会下载 `chi_sim` 语言包。**待办**：发布流程「下一步/发布」不在推荐页，需专门的发布标定流程（导航进上传流再 OCR）才能定位——真机做
- [ ] **设置持久化（本轮新增 A1）**：新增原生依赖 `@react-native-async-storage/async-storage` → 需 `expo prebuild` 重建；真机验：改设置 → 杀进程 → 重进设置仍在
- [ ] **电量显示（本轮新增 A2）**：真机验 WDA `GET /wda/batteryInfo` 能读到电量（需已建 session）；桌面设备页「电量」列显示正确、低电红/充电⚡
- [ ] **定时发布（A3 + 逐条时间改造）**：发布页**每条视频各自一个「发送时间」**（留空＝立即；扫描后按错峰自动填好、可逐条改；「错峰时间/全部立即」批量键）→ 到点电脑自动下发（电脑需保持开启）；真机端到点收到即发。验：同一设备不同视频设不同时间，各自到点分别发出
- [ ] **新增原生模块**需 `expo prebuild` + `pod-install` + 重建

---

## 2. 阶段3 发布 —— TikTok 上传是真机活
- [ ] `npx expo install expo-file-system expo-media-library`（发布下载入相册用）
- [ ] **`TikTokUI.publishVideo()` 真机实现**：标定 TikTok 上传流程坐标（选视频→填文案→发布），目前 mockUI 是占位，真机未适配时发布任务会报 failed
- [ ] 真机验：手机收到 publish:task → 下载入相册 → 发布 → 逐步状态回到桌面「发布进度」
- [ ] **设备改名**真机验：改名后 Hub 全网同步 + 视频文件夹改名 + 手机仍能正确收发布任务

---

## 3. management-center（桌面 Electron）—— 需真 Electron 运行时
> 渲染层逻辑都过了 vitest，但 Electron 外壳 + 本地文件夹/LAN 只能在桌面运行时验。

- [ ] **先构建** `pnpm --filter @mc/publisher build`（main.cjs require 它的 dist），再 `electron .`

### P1 内嵌 Hub + 零配置连接（本轮新增）—— 需 Electron 运行时 + 两台真机同网
> 纯逻辑已单测（startHub 冒烟/端口回退、beacon 编解码、parseHubQr、LAN IP 选取）；下列需运行时/真机验。
- [ ] **桌面内嵌 Hub 启动**：`electron .` 前先建 CJS dist —— `pnpm --filter @mc/shared build && pnpm --filter @mc/hub build`（main.cjs `require("@mc/hub")` 需要）；启动后主进程日志有内嵌 Hub、渲染层自动连 `localhost:<port>`（设置页「连接手机」显示已连接、不再手填地址）
- [ ] **二维码**：设置页显示 `http://<本机局域网IP>:<port>` 二维码；换机/路由重启后 IP 变，二维码随之更新
- [ ] **端口占用回退**：按共享端口表 `HUB_PORTS`（`[4000, 51820, …]`）挑第一个空闲端口；二维码/连接随实际端口
- [ ] **UDP 自动发现**：手机与电脑同 WiFi，手机端 `react-native-udp` 收到广播 → 自动连上（免扫码）；广播被防火墙拦时退回扫码
- [ ] **手机扫码**：`expo-camera` 扫桌面二维码 → 连上并**持久化**（AsyncStorage）；重启 App 免再配。属新原生模块（expo-camera + react-native-udp），需 `expo prebuild` 重建
- [ ] **重连韧性**：Hub 未启动 / 晚于手机启动（手机开机数天后）/ Hub 重启 / IP 变 → 手机自动重连（socket.io 无限重试）；未连时手机头部显示「未连接控制中心」，本机照常运行
- [ ] **端口兜底重连（本轮新增）**：当 Hub 端口变化**且** UDP 广播被防火墙拦时，手机在「已知 IP」上按共享端口表 `HUB_PORTS` 逐个探测重连（探测用 socket.io 握手指纹，筛掉「端口被别的程序占着」）。验：让内嵌 Hub 落到表内非 4000 的端口 + 关掉广播 → 手机仍能自动接回；`HUB_PORTS` 两端（桌面 `netutil.cjs` / 手机 `hubUrl.ts`）必须一致
- [ ] **.exe 打包（后续「打包」阶段）**：electron-builder 需把 @mc/shared + @mc/hub 的 dist 一并打进包（本轮用 `electron .` 联调，未做打包）

### P3 无术语 UX + 引导（本轮新增）—— 运行时/真机验
> 纯逻辑已单测（桌面 errors.ts 人话化）；下列为 UI，需运行时看效果。
- [ ] **手机首次引导**：首启激活后弹一次「使用说明」，点「开始使用」后不再弹；主界面顶部「使用说明」可随时再看
- [ ] **手机演示模式横幅**：未接真机时顶部黄条「演示模式」；接真机后消失
- [ ] **手机概率预设**：推荐/搜索/关注监控页各概率有「克制/自然/频繁」按钮，点选即变、当前档高亮
- [ ] **手机回复模板**：占位符按钮插入 + 实时「预览」一条展开结果；时间页显示设备时区
- [ ] **桌面首启欢迎**：首次打开弹欢迎三步弹窗（只弹一次）；发布状态标签鼠标悬停有说明
- [ ] **桌面错误人话化**：断网/权限/找不到等报错显示人话+补救（采集读取/扫描/发布/二维码）
- [ ] **桌面 Guide 对齐 P1**：使用说明不再提 npm/手填地址，改「打开即开控制中心 + 扫码连接」

### R 生产级健壮性（本轮新增）—— 运行时验
> 纯逻辑已单测（手机 withTimeout、Hub relay 错误兜底）；下列需运行时看效果。
- [ ] **手机错误边界**：某界面渲染崩 → 显示「应用出错·重试」而非白屏；全局未捕获错误被记录不静默崩
- [ ] **手机引擎超时**：WDA 卡死时单批 5 分钟超时进退避，不永久挂住
- [ ] **桌面错误边界**：某页渲染崩 → 显示「页面出错·刷新」而非整窗白屏
- [ ] **桌面主进程兜底**：一处未捕获错误/拒绝被记本地日志、不带崩内嵌 Hub；端口全占→启动失败弹窗、app 仍开
- [ ] **本地崩溃日志**：`%APPDATA%/<App>/logs/app-YYYY-MM-DD.log` 按日生成，含 Hub 生命周期/未捕获/IPC 错误（买家可发日志排查）
- [ ] **Hub 错误隔离**：坏 relay 请求/存储写入失败 → Hub 记日志不崩
- [ ] **待后续（打包阶段/定服务）**：外部崩溃上报（Sentry 等）、electron 自动更新、Hub 看门狗自动重启

### P4 遥测静默 + 删买家看板（本轮新增）—— 运行时验
> 用户定：**只静默卖家遥测，删买家端看板**。桌面已删「数据看板」页 + 设置里「埋点采集地址」手填框；遥测（三端 `initTelemetry`/`track`）保留为**出厂内置 env 静默回传卖家**。纯逻辑/类型已验（桌面 47 测试 + typecheck 绿）。
- [ ] **买家端无看板**：交付版（生产构建）左栏无「数据看板」Tab、设置页无「埋点采集地址」——买家全程看不到任何「采集地址/collector」字样
- [ ] **卖家静默遥测生效**：卖家构建时设好各端遥测地址（见「4. 部署」新增项），三端事件到点汇入卖家自有 collector；未设则各端静默 no-op、不影响买家使用
- [ ] 设置页「选择文件夹」对话框、根目录扫描、各设备待发/已发
- [ ] **局域网直传**：手机与操作员同网，手机能从 LAN 直链下到视频（注意防火墙/IP）
- [ ] **Hub 中转**：跨网时 POST /relay 上传 + 手机从 Hub 下载
- [ ] 主题色切换、固定外壳滚动、卡片/动效观感
- [ ] 设备改名（抽屉内联编辑）→ 文件夹改名 + 改名记录

---

## 4. 部署 / 上线
- [ ] **Hub 部署**：always-on、**国内可访问**；`HUB_DATA_DIR` 指到持久卷（设备别名 `aliases.json` 要留存）
- [ ] Hub 内存存储（设备列表）重启会丢——如需设备列表也留存，换 Prisma/Postgres（别名已落文件）
- [ ] **license SaaS 部署**：Docker（`license-saas/`），国内可访问；建管理员、建 autotk 产品
- [ ] license SDK 改动后需手动同步 vendored 到 `autotk/src/license/sdk/`
- [ ] 各端指向生产 Hub / license 地址
- [ ] **遥测 collector 部署 + 各端出厂内置地址（P4 静默卖家遥测）**：卖家部署 `services/telemetry-collector`（Postgres），构建交付物时把地址内置进各端 env——手机 `EXPO_PUBLIC_TELEMETRY_URL`、桌面 `VITE_TELEMETRY_URL`（构建期）、license `TELEMETRY_URL`（运行期）；均可留空=关闭遥测。买家侧无任何配置入口。卖家用自有工具/查询 collector 的 `/v1/stats*` 看数据（不再随交付物给买家看板）

---

## 5. 待办需求（未开工）
- [x] ~~**全系统埋点**（统一遥测）~~ —— **方案已定并落地（P4）**：自建 collector、匿名无 PII、**静默卖家遥测**（买家侧无看板/无配置）。mobile / desktop / license 三端已接入并在发（env 开关）；**Hub 尚未接**（可选，见根 CLAUDE.md 耦合表）；剩 collector 生产部署 + 各端出厂内置地址（见「4. 部署」）
- [ ] 管理中心 M4 之外：暂无其它欠项（阶段1/M1/M2/M4/阶段2/阶段3 已全做）

---

## 备注
- 纯逻辑/协议/Hub/桌面渲染层都有自动化测试，回归靠各项目 `npm test` / `bash tests/run.sh`。
- 真机相关坐标标定是一次性、按机型存 `devices.json`（换机就要重标）。
