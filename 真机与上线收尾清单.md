# 真机与上线收尾清单

二期功能的**代码 + 单测/集成测**已完成（见 `management-center/docs/dev-phase23.md`、`autotk/src/*/README.md`）。
本清单只列**还需要真机 / Mac 构建 / 实机联调 / 上线部署**才能验收的事项，拿到手机后逐项过。

更新日期：2026-06-28。

---

## 0. 前置：WDA 在 iPhone 8 / iOS 16 上跑起来
> 没有这一步，下面所有 autotk 真机验收都做不了。详见 `autotk/WDA_STANDALONE.md`。

- [ ] 云编译出 `WebDriverAgent.ipa`（GitHub Actions，Xcode 14.3.1）
- [ ] 装到手机：免费账号走 Mac 上 Sideloadly（7 天）／付费账号走 Linux zsign（1 年）
- [ ] `go-ios image auto` 挂镜像（iOS16 不要 tunnel）+ `runwda` 起到 `:8100`
- [ ] **重新 `calibrate`**：iPhone 8 是 **375×667**，与原机 390×844 不同，坐标必须重标

---

## 1. autotk（手机端，RN/Expo）—— 需 Mac 构建
> autotk 的纯 TS 逻辑都过了 node:test；但所有 **RN 接线 + 原生模块**只能在 Mac 上 `expo` 构建后真机验。

- [ ] `expo start` / dev build 跑通：配置页（关键词/推荐/搜索/个人主页/时间/日志）、启动/停止
- [ ] **后台保活**（keepalive：始终定位）真机验——退后台 30s 不被挂起
- [ ] **#2 固定回复**：UI 列表 + 占位符 `{emoji}/{user}/{a|b|c}/{kw}` 真机展开
- [ ] **#4 弹窗脱困**：REPL `alert` 命令验 WDA 14.x 的 `/alert` 接口好不好使；**不行则退回 OCR**
- [ ] **应用内浮层检测**（detectPopup，已实现框架）：按真机实际界面调签名词表/区域阈值/✕坐标（`src/engine/popupDetect.ts` + `popupDismiss.ts`），看 `popup_detected`/`popup_escaped` 埋点验自动脱困成功率
- [ ] **#3 评论 OCR 回复**：真机调坐标/阈值——评论区域、行间距、爱心↔评论对齐、Reply 入口坐标；看「评论解析:N 条」日志调
- [ ] **#5 激活码门禁**：RN 接线（Keychain 存 token、激活页、心跳）真机验；上线前到 license 后台建 `autotk` 产品，把 key/secret 填进 `autotk` config 或 `.env`
- [ ] **Hub 接入**（状态/日志/批量配置/发布接收）：HubClient / useEngine / configInbox / publish 接收的 RN 接线真机验；`EXPO_PUBLIC_HUB_URL` 指向 Hub
- [ ] **新增原生模块**需 `expo prebuild` + `pod-install` + 重建

---

## 2. 阶段3 发布 —— TikTok 上传是真机活
- [ ] `npx expo install expo-file-system expo-media-library`（发布下载入相册用）
- [ ] **`TikTokUI.publishVideo()` 真机实现**：标定 TikTok 上传流程坐标（选视频→填文案→发布），目前 mockUI 是占位，真机未适配时发布任务会报 failed
- [ ] 真机验：手机收到 publish:task → 下载入相册 → 发布 → 逐步状态回到桌面「发布进度」
- [ ] **设备改名**真机验：改名后 Hub 全网同步 + 视频文件夹改名 + 手机仍能正确收发布任务

---

## 3. management-center（桌面 Electron）—— 需真 Electron 运行时
> 渲染层逻辑都过了 vitest，但 Electron 外壳 + 本地文件夹/LAN 只能在桌面运行时验。

- [ ] **先构建** `npm run build -w @mc/publisher`（main.cjs require 它的 dist），再 `electron .`
- [ ] 设置页「选择文件夹」对话框、根目录扫描、各设备待发/已发
- [ ] **局域网直传**：手机与操作员同网，手机能从 LAN 直链下到视频（注意防火墙/IP）
- [ ] **Hub 中转**：跨网时 POST /relay 上传 + 手机从 Hub 下载
- [ ] 主题色切换、固定外壳滚动、卡片/动效观感
- [ ] 设备改名（抽屉内联编辑）→ 文件夹改名 + 改名记录

---

## 4. 部署 / 上线
- [ ] **Hub 部署**：always-on、**国内可访问**；`HUB_DATA_DIR` 指到持久卷（设备别名 `aliases.json` 要留存）
- [ ] Hub 内存存储（设备列表）重启会丢——如需设备列表也留存，换 Prisma/Postgres（别名已落文件）
- [ ] **license SaaS 部署**：Docker（`license-saas/`），国内可访问；建管理员、建 autotk 产品
- [ ] license SDK 改动后需手动同步 vendored 到 `autotk/src/license/sdk/`
- [ ] 各端指向生产 Hub / license 地址

---

## 5. 待办需求（未开工）
- [ ] **全系统埋点**（autotk / management-center / license-server 统一遥测）——见记忆 `autotk-telemetry-plan`，先确认埋点方案（自建 vs 第三方、国内可访问、隐私合规）再做
- [ ] 管理中心 M4 之外：暂无其它欠项（阶段1/M1/M2/M4/阶段2/阶段3 已全做）

---

## 备注
- 纯逻辑/协议/Hub/桌面渲染层都有自动化测试，回归靠各项目 `npm test` / `bash tests/run.sh`。
- 真机相关坐标标定是一次性、按机型存 `devices.json`（换机就要重标）。
