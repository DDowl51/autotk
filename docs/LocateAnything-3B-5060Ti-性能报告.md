# LocateAnything-3B × RTX 5060 Ti 16G 性能报告(首轮实测)

> 日期:2026-07-10　脚本:`bench/locateanything/bench.py`　样本:仓库内置 `bench/locateanything/shots/`(10 张 iPhone 8 真机 TikTok 截图,750×1334)
> 定位:为「内网集中识别服务器」选型提供**实测**依据(此前只有 A100@4K 的不相干官方数)。

## TL;DR

| 指标 | 实测值 | 判定 |
|---|---|---|
| 单张中位延迟(batch1) | **477.7 ms** | ✅ 可用(兜底式调用绰绰有余) |
| 单流吞吐 | **2.09 张/秒** | 保守下限(见外推) |
| 峰值显存 | **8.40 / 16 GB** | ✅ 富余,可批处理 |
| 定位精度(关键弹窗目标) | **与手工测量吻合 ~1%** | ✅ **选型通过的决定性证据** |
| 本配置单卡承载(混合兜底) | **38–75 台** | 生产配置外推 **100–290 台** |

**结论:模型选型通过。** 一句文字指令即可精准定位 ×/Don't Allow/点赞键——正是弹窗关闭与兜底识别所需。生产部署须 Linux(见「限制」)。

## 1. 测试环境

| 项 | 值 |
|---|---|
| GPU | NVIDIA GeForce RTX 5060 Ti 16GB(Blackwell,sm_120) |
| OS / 运行时 | **Windows 原生**(⚠️ 模型官方 Linux only,本轮为可行性验证配置) |
| PyTorch | 2.11.0+cu128 |
| transformers | 4.57.1(+ decord / lmdb / torchvision;huggingface_hub<1.0) |
| 精度 | BF16(权重 ~6.8GB) |
| 注意力后端 | **dense SDPA**(`--attn sdpa`,无 flash-attn——Windows 装不上) |
| 输入 | 长边缩至 **768**(`--max-side 768`;原生 750×1334 在 dense SDPA 下 OOM,见「限制」) |
| 生成参数 | `generation_mode="hybrid"`,`max_new_tokens=64`,`use_cache=True`,batch=1 |
| 计时 | 每张 10 次取中位,预热 2 次不计入,`torch.cuda.synchronize` 前后夹 |

## 2. 逐张结果(延迟 + 定位框)

| 截图 | 定位指令(英文提示词) | 中位延迟 | 返回框(归一化‰) | 框中心 |
|---|---|---|---|---|
| IMG_0001 | the close button X | 438.5 ms | (796,45,851,81) | (0.82, 0.06) |
| **IMG_0002** | the close button X below the card | 477.1 ms | (474,770,528,801) | **(0.50, 0.79)** |
| IMG_0003 | the like heart icon of the first comment | 474.1 ms | (746,650,806,686) | (0.78, 0.67) |
| **IMG_0007** | the close button X at the top-left of the bottom sheet | 507.7 ms | (38,736,102,772) | **(0.07, 0.75)** |
| **IMG_0008** | the Don't Allow button | 475.0 ms | (140,783,864,850) | 底部整行 (0.81) |
| comment | the like button on the right rail | 513.6 ms | (717,412,786,446) | (0.75, 0.43) |
| location-comment | the cancel or dismiss button | 478.3 ms | (879,423,938,460) | (0.91, 0.44) |
| location-popup | the cancel or dismiss button | 477.2 ms | (888,345,949,380) | (0.92, 0.36) |
| search-result | the like button on the right rail | 607.5 ms | (835,733,888,765) | (0.86, 0.75) |
| stream-video | (用例定义错,见 3.2) | 514.2 ms | — | — |

延迟分布:**min 438 / 中位 478 / 均值 496 / max 608 ms**——非常稳定,与目标类型无关。

## 3. 精度验证

### 3.1 关键弹窗目标:与手工测量对照(决定性)

本会话早前曾用像素工具手工量过三个最难弹窗的关闭目标,与模型输出对照:

| 目标 | 手工测量 | 模型定位 | 偏差 |
|---|---|---|---|
| IMG_0002 Shop 促销卡下方 × | (0.500, 0.785) | (0.501, 0.786) | **<0.2%** |
| IMG_0007 内嵌网页左上 × | (0.069, 0.755) | (0.070, 0.754) | **<0.2%** |
| IMG_0008 定位权限 Don't Allow | 底部按钮行 y≈0.81 | 行框 y 0.78–0.85 | **命中** |

这三个正是此前只能硬编码坐标、且真机上反复出问题的目标。**模型一句话定位,精度等同人工标定。**

### 3.2 一处用例定义错误(非模型错误)

`stream-video.png` 原用例让模型找「点赞键」——但该图的**业务意图是识别「直播中」徽标 → 判定直播卡 → 直接划走**。属测试定义错误,已修正(bench CASES 改为 `the "直播中" (LIVE) badge label`),待复测。其余 9 张按需求方确认无须逐一复核。

## 4. 显存

- 峰值 **8.40 GB**(权重 6.8 + 激活 ~1.6 @768/batch1),16GB 卡余量 ~7.6GB。
- 估算批处理上限(768、BF16):**batch 3–4**;换 FP8 权重(~3.4GB)后 **batch 6–8**。
- ⚠️ 原生 750×1334 + dense SDPA:单个注意力算子即需 **13.9GB** → OOM。这是 Windows(无 flash-attn)的硬限制,不是模型问题。**而 768 缩放下精度已达标(§3.1),生产可直接锁定 768 输入,绕开此问题。**

## 5. 承载量换算

`可带手机数 ≈ 吞吐 × 每台调用间隔 × 0.6 余量`

### 5.1 本配置(Windows floor,2.09 张/秒)

| 每台 VLM 调用间隔 | 用法 | 可带手机数 |
|---|---|---|
| 4–5 s | 纯 VLM 每步 | **5–6 台** |
| 30 s | 混合·兜底 | **38 台** |
| 60 s | 混合·稀疏兜底 | **75 台** |

### 5.2 生产配置外推(Linux + flash-attn + 批处理 + FP8)

三层可叠加加速:批处理 batch 6–8(~3–5×)、flash-attn(省显存+提速)、Blackwell 原生 FP8(~1.5–2×)→ 合计 **~4–8×,即 8–16 张/秒**:

| 用法 | 可带手机数 |
|---|---|
| 纯 VLM 每步 | ~20–40 台 |
| **混合·兜底(推荐架构)** | **~100–290 台** ✅ |

## 6. 限制与星号(必读)

1. **本轮是「可行性 + 精度」验证,不是生产性能**。Windows + dense SDPA 是模型的最慢形态;官方 Linux only 的实质 = 显存效率依赖 flash-attn。
2. 延迟数基于 **768 缩放输入**;所幸精度在 768 下已达标,生产可沿用。
3. batch1 单流;批处理吞吐为估算,**待下一轮实测**。
4. FP8 对 grounding 坐标精度的影响未测,启用前须用 `out/boxed_*` 复核。

## 7. 「VLM = 唯一指令源」模式的承载测算(需求方 2026-07-10 定的模式)

**模式定义**:LocateAnything 是**下一步操作坐标的唯一来源**(每个动作步都调一次);OCR 只作**文本读取**工具(评论区/文案),不参与决策。即最耗 GPU 的「纯 VLM 每步」。

### 7.1 每台设备的最小调用间隔 ≈ 1.5–2 s

瓶颈不是 VLM,是**手机物理回路**(拿不到坐标不能下一步):

| 环节 | 耗时 | 备注 |
|---|---|---|
| 截图拉取(WDA/WiFi) | 150–250 ms | JPEG 比 PNG 省 |
| VLM 推理(感知) | 200–350 ms | 生产单次 + 高并发排队 |
| 下发动作 | 100–200 ms | WDA /actions |
| **UI 落定 settle** | **500–1000 ms** | 等新页渲染,大头 |
| **合计** | **~1–1.8 s 下限,现实 ~1.5–2.5 s** | 即便 VLM 瞬回也快不过 settle |

### 7.2 单主机承载 ≈ 20–40 台

`N ≈ GPU吞吐(张/秒) × 每台间隔(秒) × 0.65 余量`,吞吐取生产外推 12–18 张/秒:

| 配置 | 吞吐 | 间隔 | 承载 |
|---|---|---|---|
| 保守 | 12 张/s | 3 s | ~23 台 |
| 中间 | 15 张/s | 2.5 s | ~24 台 |
| 激进(压到 512) | 22 张/s | 2 s | ~29 台 |

**结论:纯 VLM 每步 ≈ 单卡 20–40 台**(激进 ~50),远低于混合模式的 100+。这是「唯一指令源」这个干净架构的成本——每步烧一次 GPU。**要几百台 = 多张卡**(如 5×5060 Ti ≈ 100–200 台),与「一卡几百台」是两个成本量级。此账须在选型时认清。

### 7.3 压缩:两个独立旋钮

- **下采样(降分辨率)= 提 VLM 速度的主杠杆**。成本 ∝ 视觉 token ∝ 像素。现已 768;768→512 像素↓~56%,延迟↓~30–45%、吞吐↑~1.4×。**有精度地板**:× 按钮在 512 下仅 ~20px,须实测 IMG_0007/0002 是否还准。
- **JPEG 编码 = 省网络,不省 VLM**。PNG→JPEG(q85)字节↓~5×、截图拉取更快、缓解 WiFi;但模型看解码后像素,不改计算量。轻微伪影,q85 一般无感。
- **两者叠加**:下采样提承载,JPEG 提网络/截图。

### 7.4 OCR/VLM 分工与 GPU 共驻

- VLM(LocateAnything):**坐标/决策**——每步 1 次。
- OCR(PaddleOCR-GPU 或 CPU):**文本读取**——仅评论/文案步,~30–80ms,负载增量小(~10–20%)。
- 显存:LA BF16 6.8GB(或 FP8 3.4GB)+ PaddleOCR ~1–2GB + 批处理激活,16GB 可容;紧张则 OCR 上 CPU。

## 8. 下一轮测试计划(拿生产真值)

1. **下采样精度地板**:`--max-side 640 / 512` 各跑一遍,对比 `boxed_IMG_0007/0002` 的 × 是否仍准——**决定 §7.2 承载能提多少,优先做**。
2. **WSL2/Linux + flash-attn**:同 10 张,对照延迟与显存(原生分辨率能否低显存跑)。
3. **批处理扫描**:batch 1/2/4/8 吞吐曲线,找单卡最优点。
4. **FP8**:vLLM/TensorRT-LLM 加载 FP8,复核精度(±1% 内为过)再测吞吐。
5. **直播徽标复测**:修正后的 stream-video 用例。
6. 数据回填《内网集中识别服务器-架构设计》§6 与 D1,承载量定案。
